apiVersion: 1

providers:
  - name: ServerScope
    orgId: 1
    folder: ServerScope
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
